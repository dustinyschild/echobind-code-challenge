// poorman's environment config, this should serve my needs for now. No need to over complicate

const config = {
  rickAndMortyApi: {
    baseUrl: "https://rickandmortyapi.com/api"
  }
};

export default config;
